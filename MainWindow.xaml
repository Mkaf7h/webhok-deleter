<Window x:Class="WebhookGUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WEBHOOK DELETER" Height="650" Width="1000"
        Background="#000000" Foreground="White"
        WindowStyle="SingleBorderWindow"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
       
        <Style x:Key="CyberButton" TargetType="Button">
            <Setter Property="Background" Value="#0A0000"/>
            <Setter Property="Foreground" Value="#FF0000"/>
            <Setter Property="BorderBrush" Value="#FF0000"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF0000"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#AA0000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

       
        <Style x:Key="CyberInput" TargetType="TextBox">
            <Setter Property="Background" Value="#050000"/>
            <Setter Property="Foreground" Value="#FF4444"/>
            <Setter Property="BorderBrush" Value="#440000"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="Red"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF0000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#FF0000" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"/>
                <RowDefinition Height="*"/>  
                <RowDefinition Height="30"/> 
            </Grid.RowDefinitions>

           
            <Grid Grid.Row="0" Background="#0A0000">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                    <TextBlock Text="WEBHOOK DELETER" Foreground="White" FontSize="22" FontWeight="Black"/>
                    <Rectangle Width="2" Height="20" Fill="#FF0000" Margin="15,0"/>
                    <TextBlock Text="Horor Mkaf7h" Foreground="#FF0000" VerticalAlignment="Center" FontSize="12" FontWeight="Bold"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="10">
                    <Button Content="-" Width="40" Height="30" Style="{StaticResource CyberButton}" BorderThickness="0" Click="MinimizeButton_Click" FontSize="18"/>
                    <Button Content="X" Width="40" Height="30" Style="{StaticResource CyberButton}" BorderThickness="0" Click="CloseButton_Click" Margin="5,0,0,0"/>
                </StackPanel>
            </Grid>

            
            <Grid Grid.Row="1" Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="350"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

               
                <Border Grid.Column="0" BorderBrush="#33FF0000" BorderThickness="1" Background="#050000" Padding="20">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/> <!-- Title -->
                            <RowDefinition Height="Auto"/> <!-- Stats -->
                            <RowDefinition Height="*"/>    <!-- Inputs & Buttons -->
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="SYSTEM CONTROLS" Foreground="#FF0000" FontWeight="Bold" FontSize="14" Margin="0,0,0,20"/>

                        <StackPanel Grid.Row="1" Margin="0,0,0,20">
                            <Grid Margin="0,5">
                                <TextBlock Text="WIPED COUNTER" Foreground="#888" FontSize="11"/>
                                <TextBlock x:Name="WipedTxt" Text="0" Foreground="White" HorizontalAlignment="Right" FontWeight="Bold"/>
                            </Grid>
                            <Grid Margin="0,5">
                                <TextBlock Text="SENT PAYLOADS" Foreground="#888" FontSize="11"/>
                                <TextBlock x:Name="SentTxt" Text="0" Foreground="White" HorizontalAlignment="Right" FontWeight="Bold"/>
                            </Grid>
                            <Separator Background="#22FF0000" Margin="0,15"/>
                        </StackPanel>

                        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                            <StackPanel>
                                <TextBlock Text="WEBHOOK URL" Foreground="#FF0000" FontSize="10" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBox x:Name="UrlInput" Style="{StaticResource CyberInput}" Margin="0,0,0,20"/>

                                <TextBlock Text="SPAM MESSAGE" Foreground="#FF0000" FontSize="10" FontWeight="Bold" Margin="0,0,0,5"/>
                                <TextBox x:Name="MsgInput" Style="{StaticResource CyberInput}" Text="WEBHOOK DELETED BY MKAF7H" Margin="0,0,0,30"/>

                                <Button Content="DELETE WEBHOOK" Style="{StaticResource CyberButton}" Height="45" Margin="0,0,0,10" Click="Delete_Click"/>
                                <Button x:Name="SpamBtn" Content="START SPAM" Style="{StaticResource CyberButton}" Height="45" Click="Spam_Click"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Border>

              
                <Border Grid.Column="1" BorderBrush="#33FF0000" BorderThickness="1" Background="#050000" Margin="20,0,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="35"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Border Background="#0A0000" BorderBrush="#33FF0000" BorderThickness="0,0,0,1">
                            <TextBlock Text="TERMINAL LOGS" Foreground="#FF0000" VerticalAlignment="Center" Margin="15,0" FontSize="11" FontWeight="Bold"/>
                        </Border>
                        <ListBox x:Name="LogBox" Grid.Row="1" Background="Transparent" BorderThickness="0" Foreground="#FF4444" FontFamily="Consolas" FontSize="13" Margin="10">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="2"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <ContentPresenter />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </Grid>
                </Border>
            </Grid>

           
            <Border Grid.Row="2" Background="#0A0000" BorderBrush="#FF0000" BorderThickness="0,1,0,0">
                <Grid Margin="20,0">
                    <TextBlock Text="STATUS: SYSTEM OPERATIONAL" Foreground="#660000" VerticalAlignment="Center" FontSize="9" FontWeight="Bold"/>
                    <TextBlock x:Name="StatusTxt" Text="READY" Foreground="#FF0000" VerticalAlignment="Center" HorizontalAlignment="Right" FontSize="10" FontWeight="Bold"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
